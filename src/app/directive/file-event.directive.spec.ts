import { FileEventDirective } from './file-event.directive';

describe('FileEventDirective', () => {
  it('should create an instance', () => {
    const directive = new FileEventDirective();
    expect(directive).toBeTruthy();
  });
});
