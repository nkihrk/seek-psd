import { PointerEventDirective } from './pointer-event.directive';

describe('PointerEventDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerEventDirective();
    expect(directive).toBeTruthy();
  });
});
