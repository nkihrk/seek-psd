import { PreventHoverPropagationDirective } from './prevent-hover-propagation.directive';

describe('PreventHoverPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventHoverPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
